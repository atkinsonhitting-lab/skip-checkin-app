# Copy to .env and fill in. Never commit .env.
PORT=3000
SESSION_SECRET=change-me-to-a-long-random-string
SKIP_API_KEY=change-me-to-a-long-random-string
DATA_DIR=./data
DB_PATH=./skip.db
# Set to "true" only for first boot to seed the coach account, then remove it.
SEED_ON_BOOT=
